# start
바이브코딩 시작

## TradingView에 붙여넣는 방법
- `strategy_vfinal.pine` 파일 **내용만** 복사해서 TradingView Pine Editor에 붙여넣어야 합니다.
- GitHub PR diff 화면에 보이는 `diff --git`, `+++`, `@@` 같은 줄은 **복사하면 안 됩니다**.
- Pine Editor의 첫 줄은 반드시 `//@version=6`부터 시작해야 합니다.
